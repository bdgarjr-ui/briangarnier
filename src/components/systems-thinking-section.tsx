export function SystemsThinkingSection() {
  return (
    <section className="border-t border-border bg-background">
      <div className="mx-auto max-w-3xl px-8 py-10 sm:px-10">
        <span className="mb-6 inline-block rounded-full border border-border px-4 py-1.5 text-sm text-muted-foreground">
          Bonus: Systems Thinking, Off the Clock
        </span>
        <h2 className="mb-2 font-serif text-xl text-foreground">Color-Coding the Chaos</h2>
        <p className="mb-6 text-xs uppercase tracking-wide text-muted-foreground">
          Self-initiated workflow redesign · warehouse operations, binding shelf
        </p>

        <div className="rounded-2xl border border-border p-6 sm:p-8">
          <div className="flex flex-col gap-3 text-[0.95rem] leading-relaxed text-foreground/90">
            <p>
              Before I had a UX vocabulary, I had a warehouse job binding carpets — and a shelf of
              binding rolls with zero color logic. Every match meant digging by hand, and a wrong
              shade meant a rug that looked wrong in someone's home.
            </p>
            <p>
              So I mapped the shelf to the color spectrum and logged it in a spreadsheet. Matching
              went from a search problem to a glance — and started surfacing patterns: what got
              reached for most, what needed restocking. I was organizing physical inventory, but I
              was really organizing <em>data</em>. I didn't have the words for it then. I do now:
              user research and information architecture, running quietly on a warehouse floor.
            </p>
          </div>
        </div>

        <p className="mt-4 text-xs text-muted-foreground">
          Why it's here: this wasn't assigned. Nobody asked. I just can't leave a broken system
          alone, professionally or off the clock.
        </p>
      </div>
    </section>
  )
}
